# Supabase (REQUIRED for local dev and Vercel)
# Get these from your Supabase project: Settings → API
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Site URL (optional, defaults to https://theartspire.com)
# This drives canonical URLs, sitemap, robots.txt, OG/Twitter tags and schema.org.
VITE_SITE_URL=https://theartspire.com

# Active store currency (optional, defaults to INR). One of:
# INR, USD, GBP, AED, SAR, QAR, KWD. See src/lib/currency.ts.
# WARNING: setting a non-INR value only works once Razorpay International is
# activated AND products have per-currency prices. Do not change casually.
VITE_ACTIVE_CURRENCY=INR

# Razorpay (Dashboard → Settings → API Keys)
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# Razorpay webhook secret (Dashboard → Settings → Webhooks → create a webhook
# pointing to https://<your-domain>/api/webhooks/razorpay, subscribe to payment.captured).
# This is DIFFERENT from RAZORPAY_KEY_SECRET.
RAZORPAY_WEBHOOK_SECRET=your_webhook_secret

# Supabase service_role key (Settings → API → service_role, SECRET — never expose client-side)
# Required for server-side payment confirmation (bypasses RLS safely after signature verification)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Resend (for order confirmation emails — https://resend.com, free tier: 100/day)
# Sign up, verify your theartspire.com domain (or use their shared sandbox domain for testing),
# then create an API key here: https://resend.com/api-keys
RESEND_API_KEY=your_resend_api_key

# ── Analytics (all optional — every call is a silent no-op when its ID is unset) ──
# Nothing is loaded or sent on /admin routes.
#
# Google Tag Manager container (Admin → Container ID), e.g. GTM-XXXXXXX.
# IMPORTANT: when this is set, gtag.js is deliberately NOT also loaded, so the
# GA4 tag must be configured INSIDE GTM (otherwise GA4 receives nothing).
# Loading both would double-count every hit.
VITE_GTM_ID=
# GA4 Measurement ID (Admin → Data Streams), e.g. G-XXXXXXXXXX.
# Used directly ONLY when VITE_GTM_ID is empty; otherwise GTM forwards events.
VITE_GA4_ID=
# Meta (Facebook) Pixel ID — Events Manager → Data Sources.
VITE_META_PIXEL_ID=
# Microsoft Clarity project ID (heatmaps + SESSION REPLAY).
# PRIVACY: keep Masking = Balanced or Strict in the Clarity dashboard so
# customer phone/email/address input is never recorded. Never loads on /admin.
VITE_CLARITY_ID=

# ── Sentry error monitoring (optional — everything no-ops if VITE_SENTRY_DSN is unset) ──
# Sentry project → Settings → Client Keys (DSN). The DSN is public/safe to ship
# in the client bundle. Used by BOTH client and server error reporting.
VITE_SENTRY_DSN=
# Performance tracing sample rate (0..1). Leave 0 for errors-only (recommended).
VITE_SENTRY_TRACES_SAMPLE_RATE=0
# Source-map upload (BUILD-TIME only, set in Vercel's build env — not needed locally).
# SENTRY_AUTH_TOKEN is SECRET: create at Sentry → Settings → Auth Tokens with
# project:releases scope. Never prefix with VITE_.
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
